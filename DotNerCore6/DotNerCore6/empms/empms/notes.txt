CREATE TABLE department (
    id serial PRIMARY KEY,
    department_name VARCHAR(50)
);

INSERT INTO department (department_name)
VALUES
    ('Engineering'),
    ('Accounts'),
    ('Sales'),
    ('HR');

CREATE TABLE employee (
    id serial PRIMARY KEY,
    employee_name VARCHAR(100) not null,
	email VARCHAR(100) not null unique,
	phone VARCHAR(11) not null,
	address VARCHAR(1000) not null,
	departmentId int,
	FOREIGN KEY(departmentId) REFERENCES department(id)
);

INSERT INTO employee (employee_name, email, phone, address, departmentId)
VALUES
    ('Shan', 'shan@gmail.com','01626','shan/address',2),
	('Shawon', 'shawon@gmail.com','01537','shawon/address',4),
	('Shawan', 'shawan@gmail.com','01711','shawan/address',3);

INSERT INTO employee (employee_name, email, phone, address, departmentId)
VALUES
    ('S9', 's9@gmail.com','01626','s9/address',10),
	('S10', 's10@gmail.com','01537','s10/address',10),
	('S11', 's11@gmail.com','01711','s11/address',10);
	
	
INSERT INTO department (department_name)
VALUES
    ('New Department_2');



create table userInfo(
	id serial primary key,
	email varchar(100) unique not null,
	username varchar(10) unique not null,
	password varchar(999) not null, 
	status int not null default 1
);